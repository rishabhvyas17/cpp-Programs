#include <iostream>
using namespace std;
int main(){
    cout<<"Enter any Character to find its ASCII value";
    char ch;
    cin>>ch;
    cout<<"The ASCII value of " << ch << " is " << int(ch);
    return 0;
}
